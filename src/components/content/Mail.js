import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Mail extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

Mail.propTypes = {};

export default Mail;
